<<Python 面向对象编程>>

OOP名词
	OO：面向对象
	OOA：面向对象分析
	OOD：面向对象设计
	OOI：xxx的实现
	OOP：xxx的编程
	OOA->OOD->OOI：面向对象的实现过程

类和对象的概念
	
	类：抽象名词，代表一个集合，共性事物
		属性（变量）
		方法（函数）

	对象：具体的事物，单个个体

	类的命名：
		遵守变量命名的规范，大驼峰，避免和系统命名相似
		使用class，由属性和方法构成
		相见class.py
		class PythonStudent():
			name = None
			age = 100
			course = "Python"

	分类： 变量 = 类名（）
	访问： obj.成员的属性名称、obj.成员的方法
		yueyue = PythonStudent()
		print(yueyue.name)
		print(yueyue.age)